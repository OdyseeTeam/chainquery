{{>partial_header}}
package swagger

import (
	"log"
	"net/http"

	sw "github.com/lbryio/chainquery/swagger/apiserver/{{apiPath}}"

    "github.com/sirupsen/logrus"
)

func InitApiServer() {
    logrus.Info("API Server started")

	router := sw.NewRouter()

	log.Fatal(http.ListenAndServe(":6300", router))
}