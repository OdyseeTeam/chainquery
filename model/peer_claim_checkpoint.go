// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// PeerClaimCheckpoint is an object representing the database table.
type PeerClaimCheckpoint struct {
	ID          uint64    `boil:"id" json:"id" toml:"id" yaml:"id"`
	PeerID      string    `boil:"peer_id" json:"peer_id" toml:"peer_id" yaml:"peer_id"`
	ClaimID     string    `boil:"claim_id" json:"claim_id" toml:"claim_id" yaml:"claim_id"`
	Checkpoint  time.Time `boil:"checkpoint" json:"checkpoint" toml:"checkpoint" yaml:"checkpoint"`
	IsAvailable bool      `boil:"is_available" json:"is_available" toml:"is_available" yaml:"is_available"`

	R *peerClaimCheckpointR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L peerClaimCheckpointL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PeerClaimCheckpointColumns = struct {
	ID          string
	PeerID      string
	ClaimID     string
	Checkpoint  string
	IsAvailable string
}{
	ID:          "id",
	PeerID:      "peer_id",
	ClaimID:     "claim_id",
	Checkpoint:  "checkpoint",
	IsAvailable: "is_available",
}

// peerClaimCheckpointR is where relationships are stored.
type peerClaimCheckpointR struct {
}

// peerClaimCheckpointL is where Load methods for each relationship are stored.
type peerClaimCheckpointL struct{}

var (
	peerClaimCheckpointColumns               = []string{"id", "peer_id", "claim_id", "checkpoint", "is_available"}
	peerClaimCheckpointColumnsWithoutDefault = []string{"peer_id", "claim_id", "is_available"}
	peerClaimCheckpointColumnsWithDefault    = []string{"id", "checkpoint"}
	peerClaimCheckpointPrimaryKeyColumns     = []string{"id"}
)

type (
	// PeerClaimCheckpointSlice is an alias for a slice of pointers to PeerClaimCheckpoint.
	// This should generally be used opposed to []PeerClaimCheckpoint.
	PeerClaimCheckpointSlice []*PeerClaimCheckpoint

	peerClaimCheckpointQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	peerClaimCheckpointType                 = reflect.TypeOf(&PeerClaimCheckpoint{})
	peerClaimCheckpointMapping              = queries.MakeStructMapping(peerClaimCheckpointType)
	peerClaimCheckpointPrimaryKeyMapping, _ = queries.BindMapping(peerClaimCheckpointType, peerClaimCheckpointMapping, peerClaimCheckpointPrimaryKeyColumns)
	peerClaimCheckpointInsertCacheMut       sync.RWMutex
	peerClaimCheckpointInsertCache          = make(map[string]insertCache)
	peerClaimCheckpointUpdateCacheMut       sync.RWMutex
	peerClaimCheckpointUpdateCache          = make(map[string]updateCache)
	peerClaimCheckpointUpsertCacheMut       sync.RWMutex
	peerClaimCheckpointUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)

// OneP returns a single peerClaimCheckpoint record from the query, and panics on error.
func (q peerClaimCheckpointQuery) OneP() *PeerClaimCheckpoint {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single peerClaimCheckpoint record from the query.
func (q peerClaimCheckpointQuery) One() (*PeerClaimCheckpoint, error) {
	o := &PeerClaimCheckpoint{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for peer_claim_checkpoint")
	}

	return o, nil
}

// AllP returns all PeerClaimCheckpoint records from the query, and panics on error.
func (q peerClaimCheckpointQuery) AllP() PeerClaimCheckpointSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all PeerClaimCheckpoint records from the query.
func (q peerClaimCheckpointQuery) All() (PeerClaimCheckpointSlice, error) {
	var o []*PeerClaimCheckpoint

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to PeerClaimCheckpoint slice")
	}

	return o, nil
}

// CountP returns the count of all PeerClaimCheckpoint records in the query, and panics on error.
func (q peerClaimCheckpointQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all PeerClaimCheckpoint records in the query.
func (q peerClaimCheckpointQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count peer_claim_checkpoint rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q peerClaimCheckpointQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q peerClaimCheckpointQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if peer_claim_checkpoint exists")
	}

	return count > 0, nil
}

// PeerClaimCheckpointsG retrieves all records.
func PeerClaimCheckpointsG(mods ...qm.QueryMod) peerClaimCheckpointQuery {
	return PeerClaimCheckpoints(boil.GetDB(), mods...)
}

// PeerClaimCheckpoints retrieves all the records using an executor.
func PeerClaimCheckpoints(exec boil.Executor, mods ...qm.QueryMod) peerClaimCheckpointQuery {
	mods = append(mods, qm.From("`peer_claim_checkpoint`"))
	return peerClaimCheckpointQuery{NewQuery(exec, mods...)}
}

// FindPeerClaimCheckpointG retrieves a single record by ID.
func FindPeerClaimCheckpointG(id uint64, selectCols ...string) (*PeerClaimCheckpoint, error) {
	return FindPeerClaimCheckpoint(boil.GetDB(), id, selectCols...)
}

// FindPeerClaimCheckpointGP retrieves a single record by ID, and panics on error.
func FindPeerClaimCheckpointGP(id uint64, selectCols ...string) *PeerClaimCheckpoint {
	retobj, err := FindPeerClaimCheckpoint(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindPeerClaimCheckpoint retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPeerClaimCheckpoint(exec boil.Executor, id uint64, selectCols ...string) (*PeerClaimCheckpoint, error) {
	peerClaimCheckpointObj := &PeerClaimCheckpoint{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `peer_claim_checkpoint` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(peerClaimCheckpointObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from peer_claim_checkpoint")
	}

	return peerClaimCheckpointObj, nil
}

// FindPeerClaimCheckpointP retrieves a single record by ID with an executor, and panics on error.
func FindPeerClaimCheckpointP(exec boil.Executor, id uint64, selectCols ...string) *PeerClaimCheckpoint {
	retobj, err := FindPeerClaimCheckpoint(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *PeerClaimCheckpoint) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *PeerClaimCheckpoint) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *PeerClaimCheckpoint) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *PeerClaimCheckpoint) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("model: no peer_claim_checkpoint provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(peerClaimCheckpointColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	peerClaimCheckpointInsertCacheMut.RLock()
	cache, cached := peerClaimCheckpointInsertCache[key]
	peerClaimCheckpointInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			peerClaimCheckpointColumns,
			peerClaimCheckpointColumnsWithDefault,
			peerClaimCheckpointColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(peerClaimCheckpointType, peerClaimCheckpointMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(peerClaimCheckpointType, peerClaimCheckpointMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `peer_claim_checkpoint` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `peer_claim_checkpoint` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `peer_claim_checkpoint` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, peerClaimCheckpointPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into peer_claim_checkpoint")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == peerClaimCheckpointMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for peer_claim_checkpoint")
	}

CacheNoHooks:
	if !cached {
		peerClaimCheckpointInsertCacheMut.Lock()
		peerClaimCheckpointInsertCache[key] = cache
		peerClaimCheckpointInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single PeerClaimCheckpoint record. See Update for
// whitelist behavior description.
func (o *PeerClaimCheckpoint) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single PeerClaimCheckpoint record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *PeerClaimCheckpoint) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the PeerClaimCheckpoint, and panics on error.
// See Update for whitelist behavior description.
func (o *PeerClaimCheckpoint) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the PeerClaimCheckpoint.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *PeerClaimCheckpoint) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	key := makeCacheKey(whitelist, nil)
	peerClaimCheckpointUpdateCacheMut.RLock()
	cache, cached := peerClaimCheckpointUpdateCache[key]
	peerClaimCheckpointUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			peerClaimCheckpointColumns,
			peerClaimCheckpointPrimaryKeyColumns,
			whitelist,
		)

		if len(wl) == 0 {
			return errors.New("model: unable to update peer_claim_checkpoint, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `peer_claim_checkpoint` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, peerClaimCheckpointPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(peerClaimCheckpointType, peerClaimCheckpointMapping, append(wl, peerClaimCheckpointPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "model: unable to update peer_claim_checkpoint row")
	}

	if !cached {
		peerClaimCheckpointUpdateCacheMut.Lock()
		peerClaimCheckpointUpdateCache[key] = cache
		peerClaimCheckpointUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q peerClaimCheckpointQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q peerClaimCheckpointQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "model: unable to update all for peer_claim_checkpoint")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o PeerClaimCheckpointSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o PeerClaimCheckpointSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o PeerClaimCheckpointSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PeerClaimCheckpointSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), peerClaimCheckpointPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `peer_claim_checkpoint` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, peerClaimCheckpointPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "model: unable to update all in peerClaimCheckpoint slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *PeerClaimCheckpoint) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *PeerClaimCheckpoint) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *PeerClaimCheckpoint) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *PeerClaimCheckpoint) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("model: no peer_claim_checkpoint provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(peerClaimCheckpointColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	peerClaimCheckpointUpsertCacheMut.RLock()
	cache, cached := peerClaimCheckpointUpsertCache[key]
	peerClaimCheckpointUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			peerClaimCheckpointColumns,
			peerClaimCheckpointColumnsWithDefault,
			peerClaimCheckpointColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			peerClaimCheckpointColumns,
			peerClaimCheckpointPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("model: unable to upsert peer_claim_checkpoint, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "peer_claim_checkpoint", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `peer_claim_checkpoint` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(peerClaimCheckpointType, peerClaimCheckpointMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(peerClaimCheckpointType, peerClaimCheckpointMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to upsert for peer_claim_checkpoint")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == peerClaimCheckpointMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for peer_claim_checkpoint")
	}

CacheNoHooks:
	if !cached {
		peerClaimCheckpointUpsertCacheMut.Lock()
		peerClaimCheckpointUpsertCache[key] = cache
		peerClaimCheckpointUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteP deletes a single PeerClaimCheckpoint record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *PeerClaimCheckpoint) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single PeerClaimCheckpoint record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *PeerClaimCheckpoint) DeleteG() error {
	if o == nil {
		return errors.New("model: no PeerClaimCheckpoint provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single PeerClaimCheckpoint record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *PeerClaimCheckpoint) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single PeerClaimCheckpoint record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PeerClaimCheckpoint) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("model: no PeerClaimCheckpoint provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), peerClaimCheckpointPrimaryKeyMapping)
	sql := "DELETE FROM `peer_claim_checkpoint` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "model: unable to delete from peer_claim_checkpoint")
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q peerClaimCheckpointQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q peerClaimCheckpointQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("model: no peerClaimCheckpointQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "model: unable to delete all from peer_claim_checkpoint")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o PeerClaimCheckpointSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o PeerClaimCheckpointSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("model: no PeerClaimCheckpoint slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o PeerClaimCheckpointSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PeerClaimCheckpointSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("model: no PeerClaimCheckpoint slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), peerClaimCheckpointPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `peer_claim_checkpoint` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, peerClaimCheckpointPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "model: unable to delete all from peerClaimCheckpoint slice")
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *PeerClaimCheckpoint) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *PeerClaimCheckpoint) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *PeerClaimCheckpoint) ReloadG() error {
	if o == nil {
		return errors.New("model: no PeerClaimCheckpoint provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PeerClaimCheckpoint) Reload(exec boil.Executor) error {
	ret, err := FindPeerClaimCheckpoint(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *PeerClaimCheckpointSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *PeerClaimCheckpointSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PeerClaimCheckpointSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("model: empty PeerClaimCheckpointSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PeerClaimCheckpointSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	peerClaimCheckpoints := PeerClaimCheckpointSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), peerClaimCheckpointPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `peer_claim_checkpoint`.* FROM `peer_claim_checkpoint` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, peerClaimCheckpointPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&peerClaimCheckpoints)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in PeerClaimCheckpointSlice")
	}

	*o = peerClaimCheckpoints

	return nil
}

// PeerClaimCheckpointExists checks if the PeerClaimCheckpoint row exists.
func PeerClaimCheckpointExists(exec boil.Executor, id uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `peer_claim_checkpoint` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if peer_claim_checkpoint exists")
	}

	return exists, nil
}

// PeerClaimCheckpointExistsG checks if the PeerClaimCheckpoint row exists.
func PeerClaimCheckpointExistsG(id uint64) (bool, error) {
	return PeerClaimCheckpointExists(boil.GetDB(), id)
}

// PeerClaimCheckpointExistsGP checks if the PeerClaimCheckpoint row exists. Panics on error.
func PeerClaimCheckpointExistsGP(id uint64) bool {
	e, err := PeerClaimCheckpointExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// PeerClaimCheckpointExistsP checks if the PeerClaimCheckpoint row exists. Panics on error.
func PeerClaimCheckpointExistsP(exec boil.Executor, id uint64) bool {
	e, err := PeerClaimCheckpointExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
