// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package model

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// ClaimCheckpoint is an object representing the database table.
type ClaimCheckpoint struct {
	ID            uint64    `boil:"id" json:"id" toml:"id" yaml:"id"`
	ClaimID       string    `boil:"claim_id" json:"claim_id" toml:"claim_id" yaml:"claim_id"`
	Checkpoint    time.Time `boil:"checkpoint" json:"checkpoint" toml:"checkpoint" yaml:"checkpoint"`
	IsAvailable   bool      `boil:"is_available" json:"is_available" toml:"is_available" yaml:"is_available"`
	HeadAvailable bool      `boil:"head_available" json:"head_available" toml:"head_available" yaml:"head_available"`
	SDAvailable   bool      `boil:"s_d_available" json:"s_d_available" toml:"s_d_available" yaml:"s_d_available"`

	R *claimCheckpointR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L claimCheckpointL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ClaimCheckpointColumns = struct {
	ID            string
	ClaimID       string
	Checkpoint    string
	IsAvailable   string
	HeadAvailable string
	SDAvailable   string
}{
	ID:            "id",
	ClaimID:       "claim_id",
	Checkpoint:    "checkpoint",
	IsAvailable:   "is_available",
	HeadAvailable: "head_available",
	SDAvailable:   "s_d_available",
}

// claimCheckpointR is where relationships are stored.
type claimCheckpointR struct {
}

// claimCheckpointL is where Load methods for each relationship are stored.
type claimCheckpointL struct{}

var (
	claimCheckpointColumns               = []string{"id", "claim_id", "checkpoint", "is_available", "head_available", "s_d_available"}
	claimCheckpointColumnsWithoutDefault = []string{"claim_id", "is_available", "head_available", "s_d_available"}
	claimCheckpointColumnsWithDefault    = []string{"id", "checkpoint"}
	claimCheckpointPrimaryKeyColumns     = []string{"id"}
)

type (
	// ClaimCheckpointSlice is an alias for a slice of pointers to ClaimCheckpoint.
	// This should generally be used opposed to []ClaimCheckpoint.
	ClaimCheckpointSlice []*ClaimCheckpoint

	claimCheckpointQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	claimCheckpointType                 = reflect.TypeOf(&ClaimCheckpoint{})
	claimCheckpointMapping              = queries.MakeStructMapping(claimCheckpointType)
	claimCheckpointPrimaryKeyMapping, _ = queries.BindMapping(claimCheckpointType, claimCheckpointMapping, claimCheckpointPrimaryKeyColumns)
	claimCheckpointInsertCacheMut       sync.RWMutex
	claimCheckpointInsertCache          = make(map[string]insertCache)
	claimCheckpointUpdateCacheMut       sync.RWMutex
	claimCheckpointUpdateCache          = make(map[string]updateCache)
	claimCheckpointUpsertCacheMut       sync.RWMutex
	claimCheckpointUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)

// OneP returns a single claimCheckpoint record from the query, and panics on error.
func (q claimCheckpointQuery) OneP() *ClaimCheckpoint {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single claimCheckpoint record from the query.
func (q claimCheckpointQuery) One() (*ClaimCheckpoint, error) {
	o := &ClaimCheckpoint{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: failed to execute a one query for claim_checkpoint")
	}

	return o, nil
}

// AllP returns all ClaimCheckpoint records from the query, and panics on error.
func (q claimCheckpointQuery) AllP() ClaimCheckpointSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all ClaimCheckpoint records from the query.
func (q claimCheckpointQuery) All() (ClaimCheckpointSlice, error) {
	var o []*ClaimCheckpoint

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "model: failed to assign all query results to ClaimCheckpoint slice")
	}

	return o, nil
}

// CountP returns the count of all ClaimCheckpoint records in the query, and panics on error.
func (q claimCheckpointQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all ClaimCheckpoint records in the query.
func (q claimCheckpointQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "model: failed to count claim_checkpoint rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q claimCheckpointQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q claimCheckpointQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "model: failed to check if claim_checkpoint exists")
	}

	return count > 0, nil
}

// ClaimCheckpointsG retrieves all records.
func ClaimCheckpointsG(mods ...qm.QueryMod) claimCheckpointQuery {
	return ClaimCheckpoints(boil.GetDB(), mods...)
}

// ClaimCheckpoints retrieves all the records using an executor.
func ClaimCheckpoints(exec boil.Executor, mods ...qm.QueryMod) claimCheckpointQuery {
	mods = append(mods, qm.From("`claim_checkpoint`"))
	return claimCheckpointQuery{NewQuery(exec, mods...)}
}

// FindClaimCheckpointG retrieves a single record by ID.
func FindClaimCheckpointG(id uint64, selectCols ...string) (*ClaimCheckpoint, error) {
	return FindClaimCheckpoint(boil.GetDB(), id, selectCols...)
}

// FindClaimCheckpointGP retrieves a single record by ID, and panics on error.
func FindClaimCheckpointGP(id uint64, selectCols ...string) *ClaimCheckpoint {
	retobj, err := FindClaimCheckpoint(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindClaimCheckpoint retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindClaimCheckpoint(exec boil.Executor, id uint64, selectCols ...string) (*ClaimCheckpoint, error) {
	claimCheckpointObj := &ClaimCheckpoint{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `claim_checkpoint` where `id`=?", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(claimCheckpointObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "model: unable to select from claim_checkpoint")
	}

	return claimCheckpointObj, nil
}

// FindClaimCheckpointP retrieves a single record by ID with an executor, and panics on error.
func FindClaimCheckpointP(exec boil.Executor, id uint64, selectCols ...string) *ClaimCheckpoint {
	retobj, err := FindClaimCheckpoint(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *ClaimCheckpoint) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *ClaimCheckpoint) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *ClaimCheckpoint) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *ClaimCheckpoint) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("model: no claim_checkpoint provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(claimCheckpointColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	claimCheckpointInsertCacheMut.RLock()
	cache, cached := claimCheckpointInsertCache[key]
	claimCheckpointInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			claimCheckpointColumns,
			claimCheckpointColumnsWithDefault,
			claimCheckpointColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(claimCheckpointType, claimCheckpointMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(claimCheckpointType, claimCheckpointMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `claim_checkpoint` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `claim_checkpoint` () VALUES ()"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `claim_checkpoint` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, claimCheckpointPrimaryKeyColumns))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to insert into claim_checkpoint")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == claimCheckpointMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for claim_checkpoint")
	}

CacheNoHooks:
	if !cached {
		claimCheckpointInsertCacheMut.Lock()
		claimCheckpointInsertCache[key] = cache
		claimCheckpointInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single ClaimCheckpoint record. See Update for
// whitelist behavior description.
func (o *ClaimCheckpoint) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single ClaimCheckpoint record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *ClaimCheckpoint) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the ClaimCheckpoint, and panics on error.
// See Update for whitelist behavior description.
func (o *ClaimCheckpoint) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the ClaimCheckpoint.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *ClaimCheckpoint) Update(exec boil.Executor, whitelist ...string) error {
	var err error
	key := makeCacheKey(whitelist, nil)
	claimCheckpointUpdateCacheMut.RLock()
	cache, cached := claimCheckpointUpdateCache[key]
	claimCheckpointUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			claimCheckpointColumns,
			claimCheckpointPrimaryKeyColumns,
			whitelist,
		)

		if len(wl) == 0 {
			return errors.New("model: unable to update claim_checkpoint, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `claim_checkpoint` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, claimCheckpointPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(claimCheckpointType, claimCheckpointMapping, append(wl, claimCheckpointPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "model: unable to update claim_checkpoint row")
	}

	if !cached {
		claimCheckpointUpdateCacheMut.Lock()
		claimCheckpointUpdateCache[key] = cache
		claimCheckpointUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q claimCheckpointQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q claimCheckpointQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "model: unable to update all for claim_checkpoint")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o ClaimCheckpointSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o ClaimCheckpointSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o ClaimCheckpointSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ClaimCheckpointSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("model: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), claimCheckpointPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `claim_checkpoint` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, claimCheckpointPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "model: unable to update all in claimCheckpoint slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *ClaimCheckpoint) UpsertG(updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *ClaimCheckpoint) UpsertGP(updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *ClaimCheckpoint) UpsertP(exec boil.Executor, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *ClaimCheckpoint) Upsert(exec boil.Executor, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("model: no claim_checkpoint provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(claimCheckpointColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	claimCheckpointUpsertCacheMut.RLock()
	cache, cached := claimCheckpointUpsertCache[key]
	claimCheckpointUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			claimCheckpointColumns,
			claimCheckpointColumnsWithDefault,
			claimCheckpointColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			claimCheckpointColumns,
			claimCheckpointPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("model: unable to upsert claim_checkpoint, could not build update column list")
		}

		cache.query = queries.BuildUpsertQueryMySQL(dialect, "claim_checkpoint", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `claim_checkpoint` WHERE `id`=?",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
		)

		cache.valueMapping, err = queries.BindMapping(claimCheckpointType, claimCheckpointMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(claimCheckpointType, claimCheckpointMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.Exec(cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "model: unable to upsert for claim_checkpoint")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == claimCheckpointMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRow(cache.retQuery, identifierCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "model: unable to populate default values for claim_checkpoint")
	}

CacheNoHooks:
	if !cached {
		claimCheckpointUpsertCacheMut.Lock()
		claimCheckpointUpsertCache[key] = cache
		claimCheckpointUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteP deletes a single ClaimCheckpoint record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *ClaimCheckpoint) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single ClaimCheckpoint record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *ClaimCheckpoint) DeleteG() error {
	if o == nil {
		return errors.New("model: no ClaimCheckpoint provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single ClaimCheckpoint record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *ClaimCheckpoint) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single ClaimCheckpoint record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ClaimCheckpoint) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("model: no ClaimCheckpoint provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), claimCheckpointPrimaryKeyMapping)
	sql := "DELETE FROM `claim_checkpoint` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "model: unable to delete from claim_checkpoint")
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q claimCheckpointQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q claimCheckpointQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("model: no claimCheckpointQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "model: unable to delete all from claim_checkpoint")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o ClaimCheckpointSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o ClaimCheckpointSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("model: no ClaimCheckpoint slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o ClaimCheckpointSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ClaimCheckpointSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("model: no ClaimCheckpoint slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), claimCheckpointPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `claim_checkpoint` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, claimCheckpointPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "model: unable to delete all from claimCheckpoint slice")
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *ClaimCheckpoint) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *ClaimCheckpoint) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *ClaimCheckpoint) ReloadG() error {
	if o == nil {
		return errors.New("model: no ClaimCheckpoint provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ClaimCheckpoint) Reload(exec boil.Executor) error {
	ret, err := FindClaimCheckpoint(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *ClaimCheckpointSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *ClaimCheckpointSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ClaimCheckpointSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("model: empty ClaimCheckpointSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ClaimCheckpointSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	claimCheckpoints := ClaimCheckpointSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), claimCheckpointPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `claim_checkpoint`.* FROM `claim_checkpoint` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, claimCheckpointPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&claimCheckpoints)
	if err != nil {
		return errors.Wrap(err, "model: unable to reload all in ClaimCheckpointSlice")
	}

	*o = claimCheckpoints

	return nil
}

// ClaimCheckpointExists checks if the ClaimCheckpoint row exists.
func ClaimCheckpointExists(exec boil.Executor, id uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `claim_checkpoint` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "model: unable to check if claim_checkpoint exists")
	}

	return exists, nil
}

// ClaimCheckpointExistsG checks if the ClaimCheckpoint row exists.
func ClaimCheckpointExistsG(id uint64) (bool, error) {
	return ClaimCheckpointExists(boil.GetDB(), id)
}

// ClaimCheckpointExistsGP checks if the ClaimCheckpoint row exists. Panics on error.
func ClaimCheckpointExistsGP(id uint64) bool {
	e, err := ClaimCheckpointExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ClaimCheckpointExistsP checks if the ClaimCheckpoint row exists. Panics on error.
func ClaimCheckpointExistsP(exec boil.Executor, id uint64) bool {
	e, err := ClaimCheckpointExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
